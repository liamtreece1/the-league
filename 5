"use client";

import { Flame, Sword, Trophy, Gem } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";

interface HeroGreetingProps {
  level: number;
  streak: number;
  questsCompletedToday: number;
  lootCount: number;
}

export function HeroGreeting({
  level,
  streak,
  questsCompletedToday,
  lootCount,
}: HeroGreetingProps) {
  const stats = [
    { label: "Streak", value: `${streak}d`, icon: Flame, color: "text-amber-300" },
    {
      label: "Quests Today",
      value: questsCompletedToday,
      icon: Sword,
      color: "text-cyan-300",
    },
    { label: "Loot Owned", value: lootCount, icon: Gem, color: "text-fuchsia-300" },
    { label: "Rank", value: `LVL ${level}`, icon: Trophy, color: "text-yellow-300" },
  ];

  return (
    <section className="mx-auto mt-6 max-w-7xl px-4 md:px-6">
      <div className="hud-panel relative overflow-hidden rounded-xl p-6 md:p-8">
        {/* Decorative grid lines */}
        <div
          aria-hidden
          className="pointer-events-none absolute inset-0 opacity-[0.08]"
          style={{
            backgroundImage:
              "linear-gradient(to right, oklch(0.82 0.18 220) 1px, transparent 1px), linear-gradient(to bottom, oklch(0.82 0.18 220) 1px, transparent 1px)",
            backgroundSize: "48px 48px",
          }}
        />

        <div className="relative flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
          <div>
            <p className="text-xs uppercase tracking-[0.3em] text-cyan-300/80">
              [ Incoming Transmission ]
            </p>
            <h1 className="mt-2 text-3xl font-bold tracking-tight text-glow-cyan md:text-4xl">
              Welcome back, Traveler.
            </h1>
            <p className="mt-2 max-w-xl text-sm text-muted-foreground md:text-base">
              The System has updated your objectives. Complete quests to earn XP, unlock
              loot, and ascend the ranks of the Expedition League.
            </p>
          </div>
          <div className="grid grid-cols-2 gap-3 sm:grid-cols-4 md:w-auto">
            {stats.map((s) => (
              <Card
                key={s.label}
                className="border-cyan-400/20 bg-background/40 glow-cyan-soft"
              >
                <CardContent className="flex items-center gap-3 p-3">
                  <s.icon className={`h-5 w-5 ${s.color}`} />
                  <div className="flex flex-col leading-tight">
                    <span className="text-[10px] uppercase tracking-[0.2em] text-muted-foreground">
                      {s.label}
                    </span>
                    <span className="font-mono text-sm font-semibold text-foreground">
                      {s.value}
                    </span>
