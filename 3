"use client";

import { Sparkles, Zap } from "lucide-react";
import { Progress } from "@/components/ui/progress";
import { Badge } from "@/components/ui/badge";

interface TopNavProps {
  level: number;
  xp: number;
  xpPerLevel: number;
}

export function TopNav({ level, xp, xpPerLevel }: TopNavProps) {
  const pct = Math.min(100, Math.round((xp / xpPerLevel) * 100));

  return (
    <header className="sticky top-0 z-40 hud-panel">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-3 md:flex-row md:items-center md:justify-between md:px-6">
        {/* Left: brand */}
        <div className="flex items-center gap-3">
          <div className="relative flex h-9 w-9 items-center justify-center rounded-md border border-cyan-400/60 bg-cyan-400/10 glow-cyan-soft">
            <Sparkles className="h-5 w-5 text-cyan-300" />
          </div>
          <div className="flex flex-col leading-tight">
            <span className="text-xs uppercase tracking-[0.25em] text-cyan-300/80">
              The System
            </span>
            <span className="text-base font-semibold text-glow-cyan md:text-lg">
              Loot Drop Expedition League
            </span>
          </div>
        </div>

        {/* Right: architect + level + XP bar */}
        <div className="flex flex-col items-stretch gap-2 md:items-end">
          <div className="flex items-center justify-between gap-3 md:justify-end">
            <div className="flex flex-col items-end leading-tight">
              <span className="text-[10px] uppercase tracking-[0.25em] text-muted-foreground">
                System Architect
              </span>
              <span className="text-sm font-medium text-foreground">
                @liamtreece1
              </span>
            </div>
            <Badge
              variant="outline"
              className="border-cyan-400/60 bg-cyan-400/10 text-cyan-200 glow-cyan-soft"
            >
              <Zap className="mr-1 h-3.5 w-3.5" /> Level {level}
            </Badge>
          </div>

          <div className="flex items-center gap-3 md:w-72">
            <div className="relative h-2 w-full overflow-hidden rounded-full bg-secondary/60 ring-1 ring-cyan-400/30">
              <div
                className="xp-bar-fill h-full rounded-full transition-[width] duration-700 ease-out"
                style={{ width: `${pct}%` }}
              />
            </div>
            <span className="w-20 text-right font-mono text-xs text-cyan-200">
              {xp} / {xpPerLevel}
            </span>
          </div>
        </div>
      </div>
      {/* Bottom glow line */}
      <div className="h-px w-full bg-gradient-to-r from-transparent via-cyan-400/60 to-transparent" />
      {/* Silence unused-import warnings on Progress import tree-shaking */}
      <span className="hidden"><Progress value={0} /></span>
    </header>
  );
}
